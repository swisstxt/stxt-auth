# Currently not in use
http:
  middlewares:
    cors:
      compress: {}
      headers:
        accessControlAllowOrigin: "*"
        accessControlAllowMethods:
          - GET
          - OPTIONS
          - PUT
          - POST
          - DELETE
        accessControlAllowHeaders:
          - Content-Type
          - Authorization
        addVaryHeader: true